#include"menu.h"
using namespace std;
int main()
{
    Menu obj;
    obj.Show();
    return 0;
    //in my case it required many too many relations ship between courses and result class which is passible with 
    //association.
}
